spring:
  datasource:
    url: jdbc:h2:mem:babytracker;MODE=MySQL;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password: ""
  sql:
    init:
      schema-locations: classpath:schema-test.sql
mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
jwt:
  secret: test_secret
